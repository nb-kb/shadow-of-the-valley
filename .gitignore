# Dependencies
node_modules/

# Local env / secrets — NEVER commit API keys
.env
.env.*
!.env.example
*.key

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db

# Aider (multi-model agent) local files
.aider*
!.aider.conf.yml

# Build output (if we ever add a build step)
dist/
build/
